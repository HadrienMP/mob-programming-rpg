extends common/layout
block content
    each level, index in roles
        .level
            h2 Level #{index + 1}
            each role in level
                a(href=`/${mob}/role/${role}`): img(src=`/images/roles/${role}.png` class="badge")