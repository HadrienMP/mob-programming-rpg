extends common/layout
block content
    each level, index in roles
        .level
            h2 Level #{index + 1}
            each role in level
                img(src='/images/' + role + '.png' class="badge")